import React from 'react';
import { ViewRow } from './';

const styles = {
  container: {
    height: '2rem',
    border: '1px solid blue'
  }
};


const NavBar = () => (
  <ViewRow style={styles.container}>
  </ViewRow>
);

export default NavBar;
